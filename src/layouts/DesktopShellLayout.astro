---
import BaseLayout from "./BaseLayout.astro";
import AppShell from "../components/shell/AppShell.astro";

interface Props {
  title?: string;
  description?: string;
  ogType?: "website" | "article";
  ogImage?: string;
  canonicalUrl?: string;
  currentPath: string;
  expand?: boolean;
}

const {
  title,
  description,
  ogType,
  ogImage,
  canonicalUrl,
  currentPath,
  expand
} = Astro.props;
---

<BaseLayout title={title} description={description} ogType={ogType} ogImage={ogImage} canonicalUrl={canonicalUrl}>
  <AppShell currentPath={currentPath} expand={expand}>
    <slot />
  </AppShell>
</BaseLayout>
